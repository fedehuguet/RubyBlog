<h1><%= @category.name %></h1>
<% if @posts.each do |post| %>
	<h3><%= link_to post.title, post_path %></h3>
	<p><%= post.body %></p>
<% end.empty? %>
	<p>Sorry, no posts in category </p>
<% end %>
<%= link_to "Back to Categories", catergories_path %>